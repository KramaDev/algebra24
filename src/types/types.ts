export type MovieType = {
  id: string;
  name: string;
  year: string;
  genre: string[];
  href: string;
};

export type TabType = {
  id: string;
  label: string;
};

export type CastType = {
  id: string;
  realName: string;
  castName: string;
  image: string;
};
